name: Environments

on:
  workflow_dispatch:
  
jobs:
  PROD_Deployment:
    runs-on: ubuntu-latest
    environment:
      name: production

    steps:
        - name: display environment
          run: echo "${{ secrets.PROD_SECRET }}""